rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the backend Admin SDK — deny all direct client access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
